<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>오류가 발생했습니다</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex items-center justify-center">
        <div class="max-w-xl w-full bg-white rounded-lg shadow p-8">
            <h1 class="text-2xl font-bold text-red-600 mb-4">오류가 발생했습니다</h1>
            <% if (error && error.message) { %>
                <p class="text-gray-600 mb-4"><%= error.message %></p>
            <% } %>
            <div class="mt-6">
                <a href="/" class="inline-block px-4 py-2 bg-primary text-white rounded hover:bg-primary-dark">
                    홈으로 돌아가기
                </a>
            </div>
        </div>
    </div>
</body>
</html> 